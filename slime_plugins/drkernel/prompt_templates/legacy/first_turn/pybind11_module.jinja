You are a PyTorch and CUDA expert. Optimize the given PyTorch `Model` by returning a CUDA extension implementation that is faster than the original while preserving correctness.

Do not use inline CUDA strings or call `torch.utils.cpp_extension.load` / `load_inline` yourself.

Output exactly this section structure:

### CUDA_KERNELS
```cpp
<CUDA .cu source: kernels, device helpers, and C ABI launcher functions>
```

### APPLY_BINDINGS
```cpp
<C++/pybind source that defines a single PYBIND11_MODULE block exposing your launchers>
```

### MODEL_NEW
```python
<Python ModelNew implementation using cuda_extension>
```

CUDA_KERNELS requirements:
- Implement the core computation with real CUDA C++ code. Prefer custom `__global__` kernels.
- You may use cuBLAS for GEMM and cuDNN for convolution when appropriate; no other third-party compute libraries.
- Do not use PyTorch, ATen, or Python APIs in this section.
- Expose launcher functions callable from APPLY_BINDINGS, typically with raw pointers, sizes/strides, scalar parameters, and `cudaStream_t`.

APPLY_BINDINGS requirements:
- Define exactly one `PYBIND11_MODULE(TORCH_EXTENSION_NAME, m) { ... }` block that exposes your launcher wrappers via `m.def(...)`.
- This section is the Python/CUDA boundary only. It may validate tensors, inspect shapes/strides/dtypes/devices, allocate outputs, get `data_ptr`, get the current CUDA stream, and call C ABI launchers.
- Keep launcher declarations simple and stable: raw pointers, sizes/strides, scalar parameters, and `cudaStream_t`. Do not expose `cublasHandle_t`, cuDNN descriptors, Python objects, or PyTorch/ATen objects in the C ABI launcher signature.
- Use this stream pattern exactly unless there is a concrete reason not to:
  `cudaStream_t stream = c10::cuda::getCurrentCUDAStream().stream();`
  Do not pass a `torch::Tensor`, `c10::Device`, or `torch::Device` object to `getCurrentCUDAStream`.
- Example:
  ```cpp
  #include <torch/types.h>
  #include <torch/csrc/utils/pybind.h>
  #include <cuda_runtime.h>
  #include <c10/cuda/CUDAStream.h>

  extern "C" void my_kernel_launcher(float* out, const float* in, int n, cudaStream_t stream);

  torch::Tensor my_kernel_forward(torch::Tensor input) {
      TORCH_CHECK(input.is_cuda(), "input must be CUDA");
      auto output = torch::empty_like(input);
      cudaStream_t stream = c10::cuda::getCurrentCUDAStream().stream();
      my_kernel_launcher(output.data_ptr<float>(), input.data_ptr<float>(), input.numel(), stream);
      return output;
  }

  PYBIND11_MODULE(TORCH_EXTENSION_NAME, m) {
      m.def("my_kernel_forward", &my_kernel_forward, "my kernel forward");
  }
  ```
- Do NOT use `REGISTER_BINDING` or include `binding_registry.h`; this template uses standard pybind11 directly.
- You may use `torch::Tensor`, `TORCH_CHECK`, tensor metadata, `data_ptr`, allocation such as `torch::empty` / `torch::empty_like`, and `c10::cuda::getCurrentCUDAStream()` for the Python/CUDA boundary.
- Do not use PyTorch or ATen compute operators such as `torch::matmul`, `at::matmul`, `torch::softmax`, `at::conv`, or native operator dispatch to implement the model logic.
- Do not call CUDA kernels from Python directly. Python calls `cuda_extension.*`; `APPLY_BINDINGS` calls the C ABI launchers; `CUDA_KERNELS` implements the computation.

MODEL_NEW requirements:
- Define `class ModelNew(nn.Module)`.
- Match the original `Model.__init__` and `forward` signatures.
- Preserve state_dict compatibility: parameter and buffer names, shapes, dtypes, and semantics must match the original model.
- Preserve the original module structure. If the original model has `self.conv`, `self.linear`, `self.weight`, or `self.bias`, keep the same attribute names and compatible parameter/buffer shapes.
- Do not invent new trainable parameters, do not rename parameters, and do not create parameter names containing `"."`.
- Do not assign a new `nn.Parameter` to an attribute that the original module or submodule already owns. If the original used a submodule, instantiate the same submodule and pass its `.weight` / `.bias` tensors into `cuda_extension` calls.
- Import and call the compiled extension as `cuda_extension`.
- In `forward`, perform the core computation through `cuda_extension` calls. Do not bypass the CUDA implementation with PyTorch compute ops.
- Minimal tensor bookkeeping such as shape/device/dtype inspection or layout adaptation is allowed when needed for binding compatibility.
- Example pattern for preserving parameters while replacing computation:
  ```python
  import torch
  import torch.nn as nn
  import cuda_extension

  class ModelNew(nn.Module):
      def __init__(self, ...):
          super().__init__()
          self.conv = nn.Conv2d(...)  # only if the original Model had self.conv

      def forward(self, x):
          return cuda_extension.conv_forward(x, self.conv.weight, self.conv.bias)
  ```

{{ problem }}
